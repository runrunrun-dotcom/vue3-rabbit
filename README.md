# vue3-rabbit
这是一个基于Vue3的电商练习项目，仅供个人学习使用
1.创建项目  npm init vue@latest
  Router Pinia Linter
2.安装相关包  npm install
3.项目新增 apis(api接口文件夹) composables(组合函数文件夹) directives(全局指令文件夹) styles(全局样式文件夹) utils(工具函数文件夹)
4.基于create-vue创建出来的项目默认没有初始化git仓库，需要手动初始化  git init  git add .  git commit -m 'init'
